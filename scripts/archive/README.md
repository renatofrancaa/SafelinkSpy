# Archive — scripts one-off

Estes arquivos já foram executados (ou usados só para inspeção). **Não** rodar em produção sem revisão.

Inclui: patches de step3 (geo/cidade/gênero), encoding do funil, inspeção UTMify, mapeamento n8n/sheets, helpers de e-mail one-line.

A fonte da verdade é o código em `public/`, `app/` e `docs/n8n/`.
